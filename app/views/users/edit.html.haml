= render partial: 'shared/header'

-# パンくずリスト
- breadcrumb :addresses_edit
= render partial: 'shared/breadcrumbs'

%section.section-address
  = render partial: 'shared/sidebar'
  %main.personal-content
    .sub-content
      %section.personal-container
        %h2.title-head 本人情報の登録
        %form.text-inner
          %div
            %p.guide-text
              お客さまの本人情報をご登録ください。
              %br>/
              登録されたお名前・生年月日を変更する場合、本人確認書類の提出が必要になります。
            %p.link-text
              %a
                %span 本人確認書類のアップロードについて
                %i.fa.fa-chevron-right
          .personal-form
            %label お名前
            %p
              = "#{@user.lastname} #{@user.firstname}"
          .personal-form
            %label お名前カナ
            %p
              = "#{@user.lastname_kana} #{@user.firstname_kana}"
          .personal-form
            %label 生年月日
            %p
              = @user.birthday
          .personal-form
            %label 郵便番号
            %span.any-box 任意
            %input.input-personal-info{value: "#{@user.postal_code}",placeholder: "例) 1234567", type: "text"}/
          .personal-form
            %label 都道府県
            %span.any-box 任意
            .select-prefecture
              = collection_select :prefecture, :prefecture_id, @prefectures, :id, :name, {selected: @user.prefecture_id}
              %i.fas.fa-angle-down
                -# %option{selected: "selected"} 
    
          .personal-form
            %label 市区町村
            %span.any-box 任意
            %input.input-personal-info{value: "#{@user.city}", placeholder: "例) 横浜市緑区", type: "text"}/
          .personal-form
            %label 番地
            %span.any-box 任意
            %input.input-personal-info{value: "#{@user.city_block}", placeholder: "例) 青山1-1-1", type: "text"}/
          .personal-form
            %label 建物名
            %span.any-box 任意
            %input.input-personal-info{value: "#{@user.building}", placeholder: "例) 柳ビル103", type: "text"}/
          = link_to "#", class: "btn-registration" do
            登録する
          .personal-form.link-text
            %a
              %span 本人情報の登録について
              %i.fa.fa-chevron-right
= render partial: 'shared/footer'